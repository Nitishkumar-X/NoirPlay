package com.theveloper.pixelplay.ui.theme

import androidx.compose.ui.graphics.Color

// ── Core Dark Palette ──────────────────────────────────────────────────────
// Deep near-black backgrounds for the liquid glass aesthetic
val PixelPlayPurpleDark    = Color(0xFF080810)   // near-black background
val PixelPlayDeepSurface   = Color(0xFF0E0C18)   // slightly lighter surface
val PixelPlayPurplePrimary = Color(0xFFB06EFF)   // vibrant violet accent
val PixelPlayPink          = Color(0xFFFF6B9D)   // warm pink accent
val PixelPlayOrange        = Color(0xFFFF8C5A)   // warm orange accent
val PixelPlayLightPurple   = Color(0xFFD9B8FF)   // soft lavender for text
val PixelPlayWhite         = Color(0xFFFFFFFF)
val PixelPlayBlack         = Color(0xFF000000)
val PixelPlaySurface       = Color(0xFF141020)   // dark elevated surface

// ── Glass Tokens ───────────────────────────────────────────────────────────
val GlassWhite      = Color(0x14FFFFFF)  // 8%  – subtle fill
val GlassWhiteMid   = Color(0x1EFFFFFF) // 12% – default fill
val GlassBorder     = Color(0x28FFFFFF) // 16% – standard rim
val GlassBorderHot  = Color(0x50FFFFFF) // 32% – active / selected rim
val GlassShimmer    = Color(0x38FFFFFF) // 22% – specular highlight

// ── Light Theme ────────────────────────────────────────────────────────────
val LightBackground        = Color(0xFFF0EDFF)
val LightSurface           = Color(0xFFF8F5FF)
val LightSurfaceVariant    = Color(0xFFE3D9F8)
val LightOnSurface         = Color(0xFF1A1035)
val LightOnSurfaceVariant  = Color(0xFF473D62)
val LightPrimary           = Color(0xFF6C4FF5)
val LightPrimaryContainer  = Color(0xFFE0D5FF)
val LightOnPrimaryContainer= Color(0xFF1E0060)
val LightOutline           = Color(0xFF7060A0)
